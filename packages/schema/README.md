# @casastudio/schema

The official JSON Schema export for `ProjectSchema` is generated at:

```text
json-schema/project.schema.json
```

Regenerate it from the repository root with:

```sh
pnpm generate:schema
```
